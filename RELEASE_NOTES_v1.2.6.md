# Release v1.2.6 (prerelease)

## Fixes
- Ensure Android/Termux containers have a generated `/etc/hosts` with localhost entries so `localhost` resolves correctly.

## Notes
- This is a prerelease intended for testing on Android/Termux.
